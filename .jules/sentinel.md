## 2025-03-01 - SSRF Via Injected HTTP Clients in Scraper Strategies

**Vulnerability:**
The web scraper strategies (`BasicHTTPStrategy` and `APIDirectStrategy`) allowed a Server-Side Request Forgery (SSRF) bypass when an external `http_client` was injected. While the default HTTP client generated by `safe_httpx_client()` contained SSRF protections, custom injected clients bypassed this boundary.

**Learning:**
Security controls embedded in factory methods (such as `safe_httpx_client`) only protect the default execution path. When a component accepts dependency injection (like `http_client=...`), input parameters (like URLs) must be explicitly validated again at the component level to maintain the security boundary.

**Prevention:**
Always re-validate inputs (e.g., using `is_safe_url(url)`) directly within the component's execution flow (like at the top of the `fetch` method) rather than assuming injected dependencies enforce the same security constraints.
